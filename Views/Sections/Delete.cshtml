@model DataSystem.Models.GLM.Section
@{
    ViewData["Title"] = "Delete";
}

<div class="card">
    <div class="card-header">
        @ViewData["Title"]
    </div>

    <div class="card-body">
        @using (Html.BeginForm("Delete", "Sections"))
        {
            @Html.HiddenFor(m => m.Id);

            <h5>Are you sure you want to delete this section?</h5>
            <hr />

            <button type="submit" class="btn btn-danger float-right">Confirm Delete</button>
            <a asp-controller="Sections" asp-action="Index" class="btn btn-light float-right">Cancel</a>
        }
    </div>
</div>